void setup(){//setup function runs once
size(300,300);//sets window size
}
void draw(){//runs over and over
  if(mousePressed){//if what then this
  background(random(255),random(204),random(0));
  delay(0);//stop motion
  }
}